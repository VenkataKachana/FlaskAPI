apiVersion: v1
kind: Service
metadata:
  name: flaskapi
spec:
  type: LoadBalancer
  selector:
    app: flaskapi
  ports:
    - protocol: TCP
      port: 80
      # targetPort: 32362  # Change this to the port used by Argo CD server
